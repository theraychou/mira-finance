# Phase F1 contains no credentials and enables no integrations.
MIRA_ENV=development
MIRA_LOG_LEVEL=info

# Drive identity and approved folder IDs are stored in the ignored
# config/drive-folders.json file during Phase F8; credentials remain external.
MIRA_WHATSAPP_GROUP_ID=
