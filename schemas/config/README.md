# Base configuration schemas

These JSON Schema documents define the public-safe Phase F11 foundation contract. Database, document generation, approved Google Drive filing, and the dedicated RC Finance WhatsApp route are enabled. The F11 pilot remains an administrator-only local workflow and adds no agent tool capability.

The private Drive destination file is validated against `schemas/drive-folders.schema.json` and remains excluded from Git. OAuth credentials and tokens remain outside the workspace.
