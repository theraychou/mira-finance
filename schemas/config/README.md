# Base configuration schemas

These JSON Schema documents define the public-safe Phase F1 foundation contract. They intentionally require every operational feature to remain disabled.

Schemas for customers, documents, monetary values, numbering, bank profiles, taxes, Drive folders, and approval workflows belong to their later implementation phases.

