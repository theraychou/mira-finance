# Configuration

`foundation.json` is a non-secret project foundation record. It is not an OpenClaw agent configuration and does not enable any integration.

Operational customer, bank, numbering, tax, Drive, and WhatsApp configuration belongs to later approved phases and must not be committed with sensitive values.

