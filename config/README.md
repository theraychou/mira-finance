# Configuration

`foundation.json` is a non-secret project foundation record. It is not an OpenClaw agent configuration and does not enable any integration.

The Phase F3 ledger schema and numbering engine are operational. Customer administration, bank, tax, Drive, and WhatsApp configuration belongs to later approved phases and must not be committed with sensitive values.
