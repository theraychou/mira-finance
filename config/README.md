# Configuration

`foundation.json` is a non-secret project foundation record. It is not an OpenClaw agent configuration and does not enable any integration.

The Phase F4 customer, entity, currency, bank, and tax registries are stored in the private SQLite ledger. Bank and tax changes require the local administrator CLI. Sensitive registry values must never be committed. Drive and WhatsApp configuration belongs to later phases.
