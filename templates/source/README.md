# Protected template originals

The six `.original.docx` files in this directory are immutable F2 source records. Their SHA-256 hashes are recorded in `config/template-inventory.json`.

Never render or edit them in place. Normalisation always starts from a verified source and writes to `templates/normalized/`.
